# YA Management System 環境変数設定
# このファイルを .env にコピーして使用してください
# .env ファイルはGit管理対象外です

# アプリケーション設定
APP_ENV=development

# セッション設定（タイムアウト時間）
SESSION_TIMEOUT_HOURS=8

# セキュリティ設定
# SSL証明書検証（開発環境でのみfalseに設定）
SSL_VERIFY_PEER=true

# CSPモード: strict（推奨）, report（移行期間）, legacy（後方互換性）
CSP_MODE=legacy

# 監査ログ署名キー（必須：32文字以上のランダム文字列）
# 生成例: openssl rand -hex 32
# または: php -r "echo bin2hex(random_bytes(32));"
AUDIT_LOG_SIGNING_KEY=

# MF API設定（マネーフォワード請求書連携）
# 環境変数で設定すると config/mf-config.json より優先されます
MF_CLIENT_ID=
MF_CLIENT_SECRET=
MF_ACCESS_TOKEN=
MF_REFRESH_TOKEN=

# 暗号化キー（顧客データ暗号化用）
# 生成例: openssl rand -base64 32
# ENCRYPTION_KEY=
